'use client';
import { useEffect, useRef, useState } from 'react';
import Link from 'next/link';
import styles from '../3d-sandbox/sandbox.module.css';
import { emptyMatchSnapshot, type MatchSnapshot } from '../../game3d/match/runtime';

export default function MatchSandbox() {
  const canvas = useRef<HTMLCanvasElement>(null); const qaStarted = useRef(false);
  const [status, setStatus] = useState<'loading' | 'ready' | 'error'>('loading'); const [message, setMessage] = useState('Loading the ring, player and CPU development characters.'); const [stats, setStats] = useState<MatchSnapshot>(emptyMatchSnapshot);
  useEffect(() => { const surface = canvas.current; if (!surface) return; let disposed = false; let runtime: { dispose: () => void } | undefined;
    void import('../../game3d/match/runtime').then(({ MatchRuntime }) => { if (disposed) return; runtime = new MatchRuntime(surface, { ready: () => { if (!disposed) setStatus('ready'); }, failure: text => { if (!disposed) { setStatus('error'); setMessage(text); } }, stats: snapshot => { if (!disposed) setStats(snapshot); } }); }).catch(error => { if (!disposed) { setStatus('error'); setMessage(error instanceof Error ? error.message : '3D match runtime failed.'); } });
    return () => { disposed = true; runtime?.dispose(); };
  }, []);
  useEffect(() => { if (process.env.NODE_ENV === 'production' || status !== 'ready' || qaStarted.current) return; const query = new URLSearchParams(window.location.search); const move = (query.get('qaMove') || '').split(',').filter(code => ['KeyW', 'KeyS', 'KeyA', 'KeyD', 'ShiftLeft'].includes(code)); const hold = Math.max(100, Math.min(5000, Number(query.get('qaHold') || 1500))); if (!move.length) return; qaStarted.current = true; const keyEvent = (type: 'keydown' | 'keyup') => move.forEach(code => window.dispatchEvent(new KeyboardEvent(type, { bubbles: true, cancelable: true, code, key: code === 'ShiftLeft' ? 'Shift' : code.slice(-1).toLowerCase() }))); const start = window.setTimeout(() => { keyEvent('keydown'); document.documentElement.dataset.phase2Qa = 'held'; window.setTimeout(() => { keyEvent('keyup'); document.documentElement.dataset.phase2Qa = 'released'; }, hold); }, 700); return () => window.clearTimeout(start); }, [status]);
  return <main className={styles.shell}><div className={styles.topbar}><Link href="/" className={styles.brand}>Q/R <span>RING LAB</span></Link><Link href="/3d-sandbox">← Phase 1 sandbox</Link></div><div className={styles.title}><div><p>PHASE 02 / WRESTLING SPACE FOUNDATION</p><h1>Two bodies. One ring.</h1></div><span className={styles.badge}>3D MATCH LAB</span></div><div className={styles.workspace}><div className={styles.viewport}><canvas ref={canvas} tabIndex={0} aria-label="3D wrestling ring with player and CPU development humanoids. WASD to move, C to switch camera." /><div className={styles.sceneLabel}><i />{status === 'ready' ? 'LIVE RING SCENE' : status === 'loading' ? 'LOADING MATCH' : 'MATCH LOAD FAILED'}<span>PLAYER + CPU · NO COMBAT</span></div>{status !== 'ready' && <output className={styles.loading}><strong>{status === 'error' ? 'The ring match could not load.' : 'Preparing the ring foundation…'}</strong><p>{message}</p>{status === 'error' && <button onClick={() => window.location.reload()}>Reload match</button>}</output>}<div className={styles.sceneFooter}><span>PHASE 02 DEVELOPMENT MATCH</span><span>{stats.cameraMode.toUpperCase()}</span></div></div><aside className={styles.sidebar}><p className={styles.eyebrow}>WRESTLING SPACE FOUNDATION</p><h2>Broadcast the matchup.</h2><p>Two generic rigged development humanoids share a raised ring. Combat is intentionally disabled until Phase 3.</p><div className={styles.keys}><kbd>W</kbd><div><kbd>A</kbd><kbd>S</kbd><kbd>D</kbd></div></div><dl className={styles.instructions}><div><dt>W / S</dt><dd>Move player</dd></div><div><dt>A / D</dt><dd>Strafe player</dd></div><div><dt>SHIFT</dt><dd>Run</dd></div><div><dt>C</dt><dd>Switch camera</dd></div></dl><p className={styles.note}>The CPU only approaches and repositions. Characters face each other, stay separated and remain inside the ring boundary.</p><div className={styles.debug}><h2>Match diagnostics</h2><dl><div><dt>FPS / frame</dt><dd data-testid="match-fps">{stats.fps.toFixed(0)} / {stats.frameMs.toFixed(1)} ms</dd></div><div><dt>Player position</dt><dd data-testid="match-player">{stats.playerX.toFixed(3)} / {stats.playerY.toFixed(3)} / {stats.playerZ.toFixed(3)}</dd></div><div><dt>Player yaw</dt><dd data-testid="match-player-yaw">{stats.playerYaw.toFixed(3)}</dd></div><div><dt>CPU position</dt><dd data-testid="match-cpu">{stats.cpuX.toFixed(3)} / {stats.cpuY.toFixed(3)} / {stats.cpuZ.toFixed(3)}</dd></div><div><dt>CPU yaw</dt><dd data-testid="match-cpu-yaw">{stats.cpuYaw.toFixed(3)}</dd></div><div><dt>Separation</dt><dd data-testid="match-separation">{stats.separation.toFixed(3)}</dd></div><div><dt>Motion / animation</dt><dd data-testid="match-motion">{stats.playerMotion} / {stats.playerAnimation} · CPU {stats.cpuMotion} / {stats.cpuAnimation}</dd></div><div><dt>Skinned meshes / bones</dt><dd data-testid="match-skeleton">{stats.skins} meshes · {stats.playerBones + stats.cpuBones} bones</dd></div><div><dt>Camera</dt><dd data-testid="match-camera">{stats.cameraMode} · {stats.cameraX.toFixed(2)} / {stats.cameraY.toFixed(2)} / {stats.cameraZ.toFixed(2)}</dd></div><div><dt>Grounded / loaded</dt><dd data-testid="match-health">{stats.grounded ? 'YES' : 'NO'} / {stats.loaded ? 'YES' : 'NO'}</dd></div></dl></div></aside></div><p className={styles.bottom}>Ring geometry, dual rigged characters and a forgiving CPU. <span>Combat begins in Phase 3.</span></p></main>;
}

